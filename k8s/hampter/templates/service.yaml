apiVersion: v1
kind: Service
metadata:
  name: {{ include "hampter.fullname" . }}
  labels:
    {{- include "hampter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hampter.selectorLabels" . | nindent 4 }}
