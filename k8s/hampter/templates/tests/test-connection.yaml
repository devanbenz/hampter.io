apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hampter.fullname" . }}-test-connection"
  labels:
    {{- include "hampter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hampter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
